{% extends 'base-admin.html.twig' %}

{% block title %}Edit Cours{% endblock %}

{% block content %}
<div class="container mt-5 pt-4">
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h1 class="h3 mb-2 text-gray-800">Create new cours</h1>
                </div>
                <div class="card-body">
                    {{ include('admin/layout/_form_cours.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>
    </div>
</div>

   {# <a href="{{ path('admin_cours_index') }}">back to list</a>#}

   {# {{ include('cours/_delete_form.html.twig') }}#}
{% endblock %}
